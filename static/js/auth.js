/**
 * ============================================================
 *  E-Hotel Management System — (Legacy / Unused File)
 *  File: static/js/auth.js
 * ============================================================
 *
 *  NOTE FOR STUDENTS:
 *  This file is NOT currently used in the project.
 *
 *  It was written for an older version of the project that used
 *  Django REST Framework with API-based login (OTP verification, etc.).
 *
 *  The CURRENT version of the project uses simple Flask form-based
 *  login (no JavaScript needed for auth). The login pages are:
 *    - templates/auth/login.html   → Customer login
 *    - templates/admin/login.html  → Admin login
 *    - templates/staff/login.html  → Staff login
 *
 *  These HTML forms submit directly to the Flask routes in:
 *    - routes/customer_routes.py   → /login  (POST)
 *    - routes/admin_routes.py      → /admin/login  (POST)
 *    - routes/staff_routes.py      → /staff/login  (POST)
 *
 *  You can safely ignore this file.
 *  It is kept here for reference only.
 */
